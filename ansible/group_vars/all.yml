---
# The shared truth. These three must be byte-identical on both hosts —
# a mismatch produces silence, not an error. See docs/networking.md.
ros_distro: jazzy
ros_domain_id: 42
ros_localhost_only: 0
rmw_implementation: rmw_cyclonedds_cpp

workspace_path: "{{ ansible_env.HOME }}/piros2"
