[defaults]
inventory = inventory.yml
interpreter_python = auto_silent
stdout_callback = yaml
# Roles live beside this file; keep lookups predictable.
roles_path = roles

[ssh_connection]
# Roughly halves the round-trips to the Pi, which matters over Wi-Fi.
pipelining = True
