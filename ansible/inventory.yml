all:
  children:
    dev:
      hosts:
        ml5:
          ansible_host: 192.168.2.109
          ansible_user: proxmox-ml5
          # This is the control node and the playbook runs from here, so skip
          # the pointless SSH round-trip to ourselves (it would need a loopback
          # key otherwise).
          ansible_connection: local
    robot:
      hosts:
        pi:
          ansible_host: 192.168.2.17
          ansible_user: bthek1
